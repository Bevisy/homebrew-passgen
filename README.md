# Bevisy Passgen

## How do I install these formulae?

`brew install bevisy/passgen/<formula>`

Or `brew tap bevisy/passgen` and then `brew install <formula>`.

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
